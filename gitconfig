[alias]
        last = log -1 HEAD
        visual = !gitk
        ignore = update-index --assume-unchanged
        unignore = update-index --no-assume-unchanged
        ignored = !git ls-files -v |grep ^h
        changed = diff HEAD^ HEAD
        undo = checkout --
